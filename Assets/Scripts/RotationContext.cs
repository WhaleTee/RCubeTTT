/// <summary>
/// Defines the rotation context.
/// </summary>
public enum RotationContext {
  /// <summary>
  /// The context relative to the parent transform.
  /// </summary>
  Local,

  /// <summary>
  /// The context relative to the world.
  /// </summary>
  Global
}